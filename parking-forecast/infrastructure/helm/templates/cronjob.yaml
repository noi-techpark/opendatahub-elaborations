# SPDX-FileCopyrightText: 2026 NOI Techpark <digital@noi.bz.it>
#
# SPDX-License-Identifier: CC0-1.0

{{- range $jobName, $job := .Values.jobs }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ printf "%s-%s" $.Release.Name $jobName }}
  labels:
    {{- include "parking-forecast.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ $jobName }}
spec:
  schedule: {{ $job.schedule | quote }}
  concurrencyPolicy: {{ $job.concurrencyPolicy | default "Forbid" }}
  successfulJobsHistoryLimit: {{ $job.successfulJobsHistoryLimit | default 3 }}
  failedJobsHistoryLimit: {{ $job.failedJobsHistoryLimit | default 3 }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "parking-forecast.selectorLabels" $ | nindent 12 }}
            app.kubernetes.io/component: {{ $jobName }}
        spec:
          restartPolicy: OnFailure
          volumes:
            - name: cache
              persistentVolumeClaim:
                claimName: {{ $.Release.Name }}-cache
            {{- if eq $jobName "predict" }}
            - name: result
              persistentVolumeClaim:
                claimName: {{ $.Release.Name }}-result
            {{- end }}
          containers:
            - name: {{ $jobName }}
              image: "{{ $.Values.image.repository }}:{{ $.Values.image.tag }}"
              imagePullPolicy: {{ $.Values.image.pullPolicy }}
              command: {{ $job.command | toJson }}
              volumeMounts:
                - name: cache
                  mountPath: {{ $.Values.cache.mountPath }}
                {{- if eq $jobName "predict" }}
                - name: result
                  mountPath: {{ $.Values.result.mountPath }}
                {{- end }}
              env:
                {{- range $key, $value := $.Values.env }}
                - name: {{ $key }}
                  value: {{ $value | quote }}
                {{- end }}
                {{- if $.Values.envSecret }}
                {{- range $key, $value := $.Values.envSecret }}
                - name: {{ $key }}
                  valueFrom:
                    secretKeyRef:
                      name: {{ printf "%s-env-secret" $.Release.Name }}
                      key: {{ $key }}
                {{- end }}
                {{- end }}
              resources:
                {{- toYaml $job.resources | nindent 16 }}
{{- end }}
